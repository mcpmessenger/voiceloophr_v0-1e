# VoiceLoopHR Production Configuration
# Copy this file to .env.local and add your actual API keys

# OpenAI API Key (for Whisper STT, GPT-4, and TTS)
OPENAI_API_KEY=your_openai_api_key_here

# ElevenLabs API Key (for TTS - optional, OpenAI TTS is primary)
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Optional: Voice ID for ElevenLabs (default: Rachel voice)
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# Supabase (for document storage and semantic search)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Google OAuth (set in Google Cloud Console and Supabase Auth → Providers → Google)
# Used by Supabase on the backend; not required client-side but included for reference
GOOGLE_OAUTH_CLIENT_ID=your_google_oauth_client_id
GOOGLE_OAUTH_CLIENT_SECRET=your_google_oauth_client_secret

# App URLs (ensure callback matches these)
NEXT_PUBLIC_APP_URL=http://localhost:3000
# OAuth redirect configured in code: ${NEXT_PUBLIC_APP_URL}/auth/callback